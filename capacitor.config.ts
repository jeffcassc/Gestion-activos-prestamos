import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Front/Gestion-activos-prestamos',
  webDir: 'www'
};

export default config;
